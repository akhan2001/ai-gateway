fastapi==0.115.6
uvicorn[standard]==0.34.0
asyncpg==0.30.0
pydantic==2.10.4
openpyxl==3.1.5
reportlab==4.2.5
sendgrid==6.11.0
# supabase's sub-packages must all match exactly -- an unpinned transitive
# resolution on Railway once pulled a supabase_auth that didn't line up with
# supabase 2.22.0 (missing supabase_auth.http_clients) and crash-looped the
# whole service at import time. These are the exact versions verified to
# import cleanly together.
supabase==2.22.0
postgrest==2.22.0
supabase-auth==2.22.0
storage3==2.22.0
realtime==2.22.0
supabase-functions==2.22.0
pytest==8.3.4
pytest-asyncio==0.25.0
