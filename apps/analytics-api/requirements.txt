fastapi==0.115.6
uvicorn[standard]==0.34.0
asyncpg==0.30.0
pydantic>=2.11.7,<3.0.0
openpyxl==3.1.5
reportlab==4.2.5
sendgrid==6.11.0
# supabase==2.22.x was yanked from PyPI ("Non fixed dependencies may cause
# breakage with future incompatible changes") -- that's what crash-looped
# analytics-api in production. 2.31.0 is current and resolves clean; verified
# in an isolated venv (fresh install + full test suite) before this commit.
supabase==2.31.0
pytest==8.3.4
pytest-asyncio==0.25.0
