# Tokenix
See .claude/skills.md for full project context, stack, conventions, and priorities.

@.claude/skills.md

The Rust proxy in `crates/` is upstream (Helicone ai-gateway); its architecture
notes are kept in CLAUDE.upstream.md. The Tokenix services are the Python apps
under `apps/gateway/` and `apps/analytics-api/`.
